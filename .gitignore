# Environment Variables
.env

# Streamlit secrets
.streamlit/secrets.toml

# Python cache and virtual envs
__pycache__/
*.py[cod]
.venv/
venv/
env/

# Data (since this is a local prototype, we might want to ignore local data if it's sensitive)
# data/
